# Generated by Okty.io
version: '3.6'
services:
    php-fpm:
        build: docker/php
        volumes:
            - './:/usr/share/nginx/html'

    nginx:
        build: docker/nginx
        ports:
            - '80:80'
        volumes:
            - './:/usr/share/nginx/html'

    mysql:
        image: mysql
        environment:
            - MYSQL_DATABASE=dm_php
            - MYSQL_USER=admin
            - MYSQL_PASSWORD=skywalk
            - MYSQL_RANDOM_ROOT_PASSWORD=yes
        volumes:
            - ./data/export:/docker-entrypoint-initdb.d
        command: --default-authentication-plugin=mysql_native_password

    pma:
        image: phpmyadmin/phpmyadmin
        environment:
            - PMA_HOST=mysql
        ports:
            - 81:80
        depends_on:
            -  mysql